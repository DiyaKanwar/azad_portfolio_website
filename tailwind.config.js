module.exports = {
  darkMode: 'class',
  content: ['./src/**/*.{js,jsx,ts,tsx}', './public/index.html'],
  theme: {
    extend: {},
  },
  plugins: [],
};


// This is a basic Tailwind CSS configuration file. It enables dark mode, specifies the content paths for purging unused styles, and extends the default theme if needed. You can add custom styles or plugins as required for your project.